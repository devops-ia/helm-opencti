{{ if .Values.clustering.frontend.secrets }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "opencti.fullname" . }}-frontend-credentials
  labels:
    {{- include "opencti.frontendLabels" . | nindent 4 }}
  annotations:
    helm.sh/hook: "pre-install,pre-upgrade"
    helm.sh/hook-weight: "0"
data:
  {{- range $key, $value := .Values.clustering.frontend.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{ end }}
