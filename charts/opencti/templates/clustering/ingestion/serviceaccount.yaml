{{- if eq .Values.deploymentType "clustering" }}
{{- if .Values.clustering.ingestion.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opencti.serviceAccountName" . }}
  labels:
    {{- include "opencti.ingestionLabels" . | nindent 4 }}
  {{- with .Values.clustering.ingestion.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.clustering.ingestion.serviceAccount.automountServiceAccountToken | default "false" }}
{{- end }}
{{- end }}
