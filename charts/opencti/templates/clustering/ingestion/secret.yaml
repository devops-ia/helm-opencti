{{ if .Values.clustering.ingestion.secrets }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "opencti.fullname" . }}-credentials
  labels:
    {{- include "opencti.ingestionLabels" . | nindent 4 }}
  annotations:
    helm.sh/hook: "pre-install,pre-upgrade"
    helm.sh/hook-weight: "0"
data:
  {{- range $key, $value := .Values.clustering.ingestion.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
{{ end }}
